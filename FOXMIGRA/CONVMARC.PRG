**  DEFINIR CAMPO TXT  COM TAMANHO 200


USE MARCA EXCL
ZAP
APPEND FROM Q:\FPD26\BASE\LST\MARCAS.TXT FIELDS TXT TYPE SDF

DELETE ALL FOR SUBS(TXT,3,3) <> " | "
DELETE ALL FOR LEFT(TXT,3)    = "   "


PACK
******>> PREENCHIMENTO DOS CAMPOS

*-*-*-*-*-*-*--*-*-*-   CONVERCAO DOS ITENS

REPLACE ALL ;
   	GRUPO  WITH INT(VAL(SUBS(TXT,1,2))),;
   	MARCA  WITH INT(VAL(SUBS(TXT,6,1))),;
	DESCRICAO	WITH SUBS(TXT,10,17)
