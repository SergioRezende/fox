SET STEP ON
use q:\scgc\central\saldo
SET ORDER TO TAG DTABERT OF Q:\SCGC\CENTRAL\SALDO.CDX
SET NEAR ON
SET TALK ON
SET ESCAPE ON
ON ERROR
SEEK DTOS({01.06.2000})
SET DATE GERMAN

DO WHILE !EOF() AND ;
		(DTABERT >= {01.06.2000} AND DTABERT < {01.07.2000})
		
		IF   (QTD_COMPRA+QTD_VENDA+QTD_E_TRAN+QTD_E_OUTR+QTD_S_TRAN+QTD_S_OUTR) = 0;
			 AND SLD_ANTE = SLD_ATU
			 =RLOCK()
			REPLACE  VLR_ATU WITH VLR_ANTE
		ENDIF
		SKIP
ENDDO
