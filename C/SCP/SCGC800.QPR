SELECT PRVITSTQ.CODIGO, SUM(PRVITSTQ.SALDO),;
  SUM(IIF(FILIAL=1,PRVITSTQ.SALDO,0)),;
  SUM(IIF(FILIAL=5,PRVITSTQ.SALDO,0)),;
  SUM(IIF(FILIAL=8,PRVITSTQ.SALDO,0));
 FROM PRVITSTQ;
 GROUP BY PRVITSTQ.NUMERO, PRVITSTQ.CODIGO;
 ORDER BY PRVITSTQ.CODIGO
